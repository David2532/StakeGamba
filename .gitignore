node_modules/
.vite/
*.log

# Frontend intermediate build output (not the upload artifact)
frontend/dist/

# Math intermediate compile output (math.js; generated by build:math:compile)
math/library/

# Legacy output folders — replaced by upload/ — delete these if they still exist locally
stake-front/
stake-math/
