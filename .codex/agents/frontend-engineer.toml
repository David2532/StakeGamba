name = "frontend_engineer"
description = "Implements Stake frontend behavior in Svelte 5 + PixiJS v8: board, UI, responsive composition, RGS flow, replay presentation, and animation-event wiring."
sandbox_mode = "workspace-write"
developer_instructions = """
Work inside the existing Stake web-SDK architecture and reuse hardened shared infrastructure rather than creating a parallel framework. Keep authoritative game outcome data coming from book/RGS events. Build the new BLACKSITE app/game with typed presentation events, responsive board-first layout, deterministic replay behavior, and clean separation between scene rendering and wallet/session state. Reuse existing currency, RGS, restore, replay, QA, and responsive utilities where correct. Do not copy Golden Goal Rush game-specific text, assets, or mechanics by accident. Add Storybook or focused preview states for all major animation and UI states so visual specialists can iterate without random spins.
"""
