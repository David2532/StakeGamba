# Prioritized release backlog

| ID               | Priority | State | Vertical slice and exit condition                                                                                                                                                                                                          |
| ---------------- | -------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| BSB-RGS-001      | High     | DONE  | Accept only the documented half-up whole-micro-unit payout for fractional centi-x products; real fixture, boundary, new-play, and restore tests pass. Commit `2977b4d`.                                                                    |
| BSB-CI-001       | High     | DONE  | Branch-aware CI, production-source typecheck, app gates, full math, Chromium E2E, exact identity and diagnostics passed on clean commit `a2a2e623` in run `33317660876`.                                                                   |
| BSB-QA-001       | High     | DONE  | Pinned GitHub Chromium executed 36/36 exact-build scenarios and 736/736 checks; desktop, 390x844, 844x390 and 360x640 Replay screenshots were inspected; console/network and fractional wallet/browser cases passed.                       |
| BSB-IPHONE-DEMO-CI-001 | High | DONE | Exact CI `33818861870` on `c4eeb96b` proves the bounded fresh-live boot intro, 44px focused mobile Skip, Reduced Motion bypass and compact Vaultkeeper request omission. A QA-only correction removes authentication from transactional-write counts, measures character motion at a rendering viewport and makes focus traversal deterministic. Chromium passes 93/93 scenarios / 3035 checks; artifact `9917922010` supplies five inspected views. Public-link and real-iPhone reachability remain unclaimed. |
| BSB-BROWSER-STORAGE-RESILIENCE-001 | High | DONE | Storage acquisition now catches a throwing browser `localStorage` getter before all motion, audio and intro persistence use. Exact CI `33824176688` passes 97/97 Chromium scenarios / 3123 checks; a 390x844 storage-denied fixture reaches `live-ready` with safe defaults, one authentication, zero paid writes and clean diagnostics. Physical iPhone/Safari sign-off remains external. |
| BSB-RELEASE-EVIDENCE-BOUNDARIES-001 | High | DONE | Exact-source CI `33824173649` and PR merge-result CI `33824176688` on `021a1d64` prove deterministic release-bundle v1, compliance v3, scale verifier v7/self-test 46/46, device-evidence v3/owner-review v1 contracts, exact-SHA package/runtime hardening and 97/97 Chromium scenarios / 3,123/3,123 checks. Artifact `9919765967` contains the exact candidate, evidence and 137 captures. A throwing `localStorage` getter falls back to safe defaults and reaches an enabled action in mobile-emulated Chromium; no paid play is executed. Hidden-document logic/wiring is unit/source-proven only, while native background/foreground, production-equivalent scale, physical-device/real-Popout/final-audio/accessibility, final assets/Spine/rights/Creative, 23 manual gates and 6 external lifecycle gates remain open. |
| BSB-ID-001       | Critical | DONE  | Penguin Vaultkeeper + physical lock/vault identity is locked across plans, manifest and director prompts; retained human concepts are superseded/runtime-ineligible; 3 regression tests enforce the contract. Commit `e243fe7`.            |
| BSB-HUD-001      | Critical | DONE  | Player-facing vault HUD replaces coordinates/placeholders/three-letter codes and internal schema/hash copy; semantic symbol state and Social-safe copy pass 75 tests plus 36/36 Chromium scenarios. Commit `cde685d`.                      |
| BSB-MOBILE-001   | High     | DONE  | Base Amount/modes/meters centered, labels wrap without clipping, focus is 3px amber + 2px offset, touch/viewport geometry and <=0.2% square aspect pass required viewports. Named route/motion groups, atomic statuses and exact high-cost descriptions pass current-package semantics. Commits `fa60a47`, `9099585`. |
| BSB-ASSET-001    | Critical | IN_PROGRESS | Penguin fallback plus independent desktop/portrait mechanical-vault plates are provenance/hash-bound, optimized and exact-browser proven. Exit remains: manual cleanup and rights/Creative approval, Spine 4.2 rig, BLACKOUT and separable foreground layers. |
| BSB-MOTION-001   | High     | IN_PROGRESS | Authoritative board/BLACKOUT/cascade motion, semantic Vaultkeeper states and deterministic seven-column reel stops have normal/turbo/reduced timing, skip, cleanup, missing-image fallback and exact-browser payout proof. Full BLACKOUT's compositor pulse improves the same exact scenario from 50.0ms to 33.4ms p95; Vaultkeeper compositor hints now exist only during active authored reactions and clean to `auto`. Exit remains: approved Spine clips and real-device frame-pacing/memory evidence. |
| BSB-MAX-WIN-HERO-TIMING-001 | High | DONE | `cap_reached` owns the complete authored Max-Win hero window instead of the generic step delay: 1,000ms Normal, 360ms Turbo and 0ms Reduced. Skip cancels cleanly; exact Chromium observes 1006.2ms / 364.3ms, exact `$10000.00` payout authority, two plays and zero checkpoint/settlement writes. Exact run `33567927794`; approved Spine clips and real-device pacing remain in `BSB-MOTION-001`. |
| BSB-RECOVER-HERO-TIMING-001 | High | DONE | Terminal `recover` owns the complete authored window instead of the generic step delay: 1,000ms Normal, 360ms Turbo and 0ms Reduced. Completed feature exits are not delayed twice and Skip cancels cleanly; exact Chromium observes 995.7ms / 357.1ms, exact `$10000.00` payout authority, two plays and zero checkpoint/settlement writes. Exact run `33572370281`; approved Spine clips and real-device pacing remain in `BSB-MOTION-001`. |
| BSB-TURBO-SPIN-CUE-TIMING-001 | High | DONE | Turbo `spin_start` now owns the complete 110ms authored Vaultkeeper reaction instead of being replaced by Monitoring after 60ms. Normal remains 160ms, Reduced remains 0ms and Skip cancels cleanly; exact Chromium observes 111.7ms Turbo / 165.0ms Normal, two exact plays and zero checkpoint/settlement writes. Exact run `33576342359`; approved Spine clips and real-device pacing remain in `BSB-MOTION-001`. |
| BSB-REPLAY-MOTION-PROFILE-001 | High | DONE | Replay no longer pins generic 16/220ms delays over the active presentation profile. Normal, Turbo and Reduced Motion now inherit the same authored step/hit grammar as live play while explicit test/embedding overrides remain supported; exact run `33580462193` proves Normal/Turbo hit ownership, one cached Replay GET and zero wallet writes. Broader Replay/provider evidence remains in `BSB-EVIDENCE-001`. |
| BSB-REPLAY-REDUCED-MOTION-EVIDENCE-001 | High | DONE | Exact run `33656188904` proves one cached Replay through Normal, Turbo and Reduced Motion. Reduced completes in 50.3ms; all four sampled frames remain board `idle`, Vaultkeeper `idle_a`, profile `reduced`, CSS `none` / `0s`, compositor `auto`, with one exact Replay GET, zero wallet writes and clean diagnostics. Chromium passes 84/84 scenarios / 2326 checks, current-SHA 51-point resolution passes, and artifact `9857355056` supplies inspected desktop/portrait/landscape captures. |
| BSB-REDUCED-MOTION-VAULTKEEPER-001 | High | DONE | The semantic Reduced Motion profile independently disables Vaultkeeper animation/transitions and releases compositor hints instead of depending on the global system-media fallback. Exact run `33584733683` activates `feature_trigger`, computes `none` / `0s` / `auto`, retains the stored Turbo preference and performs zero wallet writes. Approved Spine clips and real-device validation remain in `BSB-MOTION-001`. |
| BSB-BLACKOUT-HERO-TIMING-EVIDENCE-001 | High | DONE | Deterministic `blackout_zero` now binds both BLACKOUT trigger and recovery windows to the active profile in exact Chromium: 1000.4/1026.9ms Normal and 359.7/365.8ms Turbo against 1000/360ms CSS, with two exact 1,000,000-micro-unit BLACKOUT plays, exact `$0.00` and zero checkpoint/event/settlement writes. Exact run `33588488559`; approved layers and real-device pacing remain in `BSB-MOTION-001`. |
| BSB-CASCADE-PHASE-TIMING-EVIDENCE-001 | High | DONE | Deterministic exact Chromium now binds hit/remove/drop/settle to computed CSS and observed windows in both profiles: Turbo 110/55/105/35ms CSS → 124.5/56.9/104.3/40.2ms observed; Normal 280/150/250/90ms CSS → 312.6/152.1/249.4/92.3ms observed. Both retain 16.8ms p95 frame pacing, zero >50ms samples and exact Base request ownership in run `33593132383`; approved clips and real-device pacing remain in `BSB-MOTION-001`. |
| BSB-FEATURE-TEASE-TIMING-EVIDENCE-001 | High | DONE | Deterministic exact Chromium binds the semantic Vaultkeeper `feature_tease` and board `anticipation` lock pulse to computed CSS in both profiles: each owns 600ms Normal / 180ms Turbo and remains visible for complete observed 752.8ms / 233.5ms windows. Two exact 1,000,000-micro-unit Base plays and zero checkpoint/event/settlement writes remain authoritative in run `33597126878`; approved clips and real-device pacing remain in `BSB-MOTION-001`. |
| BSB-BOARD-REVEAL-TIMING-EVIDENCE-001 | High | DONE | Deterministic exact Chromium proves all 49 staggered reveal cells complete before the presentation advances: Normal owns 180ms animation plus 0–156ms delay within a 359.8ms observed window; Turbo owns 70ms plus 0–54ms within 128.4ms. Two exact 1,000,000-micro-unit Base plays finish at `$0.00` / `$998.00` with zero checkpoint/event/settlement writes in run `33601180214`; approved clips and real-device pacing remain in `BSB-MOTION-001`. |
| BSB-VAULTKEEPER-WIN-TIMING-EVIDENCE-001 | High | DONE | Deterministic exact Chromium binds the semantic Vaultkeeper `win_acknowledge` state to its computed animation in both profiles: Turbo computes 110ms and remains visible for 190.3ms; Normal computes 280ms and remains visible for 464.8ms before Monitoring. Dedicated hit captures, three exact Base requests and zero checkpoint/event/settlement writes pass in run `33605467946`; approved clips and real-device pacing remain in `BSB-MOTION-001`. |
| BSB-ZERO-WIN-CHARACTER-REACTION-001 | High | DONE | Authoritative zero outcomes own a distinct bounded `loss_acknowledge` response instead of the feature-recovery reaction: 320ms Normal, 130ms Turbo and 0ms Reduced. Exact Chromium observes 318.7ms / 129.4ms, clean idle compositor release, two exact `$0.00` Base outcomes and zero checkpoint/event/settlement writes in run `33612508384`; approved clips and real-device pacing remain in `BSB-MOTION-001`. |
| BSB-FEATURE-WIN-CHARACTER-REACTION-001 | High | DONE | Authoritative feature wins own `bonus_win` while Base wins retain `win_acknowledge`; the distinct one-shot response is 280ms Normal, 110ms Turbo and 0ms Reduced. Exact Chromium observes 204.2ms / 178.2ms semantic ownership in natural Base and confirmed Deep Access feature flows, exact `$1000.00` / `$2000.00` payouts and zero checkpoint/event/settlement writes in run `33621532438`; approved clips and real-device pacing remain in `BSB-MOTION-001`. |
| BSB-FEATURE-WIN-PROFILE-EVIDENCE-001 | High | DONE | Exact Chromium now exercises `bonus_win` under both authored profiles rather than forcing Turbo twice: Natural Base Normal computes 280ms and owns 528.0ms; confirmed Deep Access Turbo computes 110ms and owns 177.3ms. Selected controls, compositor cleanup, exact payouts and wallet/network isolation pass in run `33645610063`; approved clips and real-device pacing remain in `BSB-MOTION-001`. |
| BSB-FEATURE-IDLE-CONTINUITY-001 | High | DONE | Feature-phase `board_snapshot` cues preserve `bonus_idle` instead of regressing to Base `monitoring`. Previous exact evidence recorded 23 unwanted Monitoring transitions in each deterministic feature segment; exact run `33669021386` now observes 12 static idle stances, 11 bounded bonus-win reactions and zero Monitoring per Normal/Turbo segment. Exact payouts, one play and zero event/settlement writes per flow remain authoritative; 84/84 scenarios / 2328 checks pass. |
| BSB-FEATURE-MODE-BADGE-OVERLAP-001 | High | DONE | The stage heading uses a bounded two-column grid and the active mode chip is truncation-safe. Exact run `33692233510` measures a 12px gap and zero facility-kicker/heading collision at 1920x1080 and 1366x768; compact 390x844 and 844x390 intentionally hide the redundant chip. All four active-state captures were inspected; no timing, math, wallet/provider or asset behavior changed. |
| BSB-PACKAGE-HYGIENE-001 | High | DONE | The pinned inline frontend now ships an exact six-file allowlist instead of unreachable generated JS/CSS/env residue. Runtime/source asset digests, recovery metadata, physical roots, clean current commit/tree, manifest/README/toolchain fields, embedded-data/debug/source-map scans and rerun-safe verification are regression-bound. Exact CI `33685146602`: 6 files / 439,506 bytes, package/readback PASS, 177 tests, 7/7 math gates, 84/84 Chromium scenarios / 2328 checks and current-SHA resolver PASS; independent red team SHIP. |
| BSB-BASE-WIN-TIER-REACTIONS-001 | High | DONE | Authoritative Base wins map to `win_small` `<1.00x`, `win_medium` `1.00x–1.99x`, and `win_big` `>=2.00x` using safe integer raw centi-x values. Exact run `33637682367` passes 141 tests, full math/package/51-point gates and 84/84 Chromium scenarios / 2317 checks. Replay Small measures 452.2ms Normal / 153.6ms Turbo; live Small/Big measure 211.5ms / 491.7ms Turbo with exact `$0.38` / `$2.07`, two plays and zero event/settlement writes. Active Small/Medium/Big and all required viewport captures were inspected; approved Spine clips and real-device pacing remain in `BSB-MOTION-001`. |
| BSB-NORMAL-BIG-WIN-TIMING-EVIDENCE-001 | High | DONE | Exact Chromium closes the remaining Base tier/profile cell: live Normal `win_big` computes the authored 1,400ms CSS clip and remains semantically active for 1,584.2ms before clean idle release. Turbo Small/Big remain 212.7ms / 492.2ms, all three exact Base plays preserve `$0.38` / `$2.07` payouts and issue zero event/settlement writes. Run `33649656438` passes 84/84 scenarios and 2324/2324 checks; approved Spine clips and real-device pacing remain in `BSB-MOTION-001`. |
| BSB-AUDIO-001    | High     | IN_PROGRESS | Procedural master bus, exact seven-stop normal/turbo cadence, complete authoritative cue coverage, semantic Vaultkeeper/feature recipes, priority ambience duck/restore, one ambience graph, gesture policy, persisted levels/mute, visibility lifecycle, external AudioContext interruption/closure, cooldown, voice cap and teardown pass automated regression. Storage denial preserves the safe locked `FULL` default but does not prove playback. Exit remains: final approved masters plus manual clipping/listening/physical-device QA. |
| BSB-EVIDENCE-001 | High     | IN_PROGRESS | Auth/reconnect, a real 10-second auth-timeout abort/reload, dynamic authenticate levels plus fail-closed conflicting step aliases, package identity, competing input and currency families are exact-browser bound. Five deterministic wins per canonical mode prove 15 read-only Replay cases, 121 win events and 122 clusters. Natural Base and confirmed Deep Access prove feature lifecycles. Failed settlement, API-level `ERR_IPB`, expired sessions, zero- and positive-payout server-accepted/client-response-lost play, checkpoint and settlement recovery, event-driven active-feature cursor restore and active-feature Skip/settlement independently prove fail-closed completion without automatic duplicate writes; reauthentication never resubmits an uncertain accepted/rejected action and a live/restore path settles at most once. A throwing `localStorage` getter and slow-auth pre-intro ordering are exact-package browser-bound, but the storage case performs no paid play and uses mobile emulation. Hidden-document eligibility/wiring is unit-bound only; native background/foreground requires physical mobile and real Popout evidence. Compliance v3 records 38 automated-proof-complete rows and 40 rows with resolved repository evidence, including partial repository receipts on external rows 45–46. All 23 manual gates, 6 external lifecycle gates and 2 N/A remain explicit; no manual evidence, external approval, upload authorization or release readiness is claimed. |
| BSB-ACCEPTED-PLAY-RESPONSE-LOSS-001 | High | DONE | Exact Chromium lets the native mock RGS accept one `$1.00` Base play, deliberately loses only the client response during reload, then restores and settles the authoritative active round once. Exact order is `authenticate → play → authenticate → end-round`; attempts/acceptances/aborts are 1/1/1, play/event/end-round counts are 1/0/1, final balance is `$999.00`, and the expected transport abort is classified separately. Exact CI `33714953239`: 85/85 scenarios / 2384 checks; broader provider evidence remains in `BSB-EVIDENCE-001`. |
| BSB-ACCEPTED-WINNING-PLAY-RESPONSE-LOSS-001 | High | DONE | Exact Chromium lets the RGS accept one `$1.00` `base_big` play, loses only the client response, reauthenticates at the debited `$999.00`, restores 49 distinct cursors and settles the `$200.00` payout exactly once at `$1199.00`. Exact order/bodies prove one play and one settlement; session position correctly reads `−$200.00` from the reload-local `$999.00` baseline. Exact CI `33767664586`: 88/88 scenarios / 2651 checks; artifact `9898887055` visually verified. Broader provider evidence remains in `BSB-EVIDENCE-001`. |
| BSB-ACCEPTED-WINNING-CHECKPOINT-RESPONSE-LOSS-001 | High | DONE | Exact CI `33779286645` on candidate `85dcadfe` proves one accepted `base_big` first checkpoint whose response is lost, authoritative cursor restore without rewrite, 49 unique ordered cursors total, one `$200.00` settlement and `$1199.00` final balance. Exact auth/play/event/end-round counts are 2/1/49/1; Chromium passes 89/89 scenarios / 2878 checks, current-SHA resolver passes, and artifact `9903557420` supplies the four primary plus pending/restored views. Broader provider evidence remains in `BSB-EVIDENCE-001`. |
| BSB-ACCEPTED-SETTLEMENT-RESPONSE-LOSS-001 | High | DONE | Exact Chromium lets the native mock RGS accept one `/wallet/end-round`, deliberately loses only the client response during reload, then reauthenticates into the authoritative inactive round without settlement retry. Exact order is `authenticate → play → event → end-round → authenticate`; attempts/acceptances/aborts are 1/1/1, auth/play/event/end-round counts are 2/1/1/1, final balance is `$999.00` with cleared win, and the expected endpoint-bound transport abort is classified separately. Exact CI `33720790794`: 86/86 scenarios / 2406 checks; broader provider evidence remains in `BSB-EVIDENCE-001`. |
| BSB-ACCEPTED-CHECKPOINT-RESPONSE-LOSS-001 | High | DONE | Exact Chromium lets the native mock RGS accept one `/bet/event` cursor `blacksite-book-events-v1:2`, deliberately loses only the client response during reload, then resumes from the authoritative cursor without rewriting it and settles once. Exact order is `authenticate → play → event → authenticate → end-round`; attempts/acceptances/aborts are 1/1/1, auth/play/event/end-round counts are 2/1/1/1, final balance/win are `$999.00`/`$0.00`, and the expected endpoint-bound abort is separate. Exact CI `33724943687`: 87/87 scenarios / 2432 checks. |
| BSB-ACCEPTED-WINNING-SETTLEMENT-RESPONSE-LOSS-001 | High | DONE | Exact Chromium executes the authoritative `base_big` `$200.00` win, persists all 49 durable cursors exactly once, lets the RGS accept one `/wallet/end-round`, loses only that client response, and reauthenticates to the one-time `$1199.00` balance without settlement retry or double payout. Attempts/acceptances/aborts are 1/1/1; exact CI `33731906526` passes 88/88 scenarios / 2454 checks. Broader provider evidence remains in `BSB-EVIDENCE-001`. |
| BSB-SCALE-001 | High | OPEN | Schema-v7 verifier/runbook bind exact Git/frontend/math and immutable environment/provider/CDN/RGS identities, approved RPS windows/sample floors, reconciled counters, exact resilience/observability/rollback sets, expiry limits and six signed normalized-source reports with digest-readback attachments. No owner-approved production-equivalent CDN/RGS/provider load/soak/fault run exists. Exit still requires the real run, retained tool-native exports, six accepted role-owned reports, external trust store and owner acceptance; structural/self-test success cannot prove capacity. |
| BSB-SCALE-EVIDENCE-GATE-001 | High | DONE | Exact commit/package-bound evidence verifier, 9 contracts and 14-case self-test reject mock targets, missed workload/SLO/cache/saturation, duplicate wallet/settlement writes, incomplete resilience/observability/rollback and invalid digests. Exact run `33696034709`; this closes tooling only and makes no capacity claim. |
| BSB-SCALE-EVIDENCE-INTEGRITY-001 | High | DONE | Schema v2 binds approval before execution, declared phase duration to the actual run window, measured request totals to endpoint samples, and six unique role/run/size/digest-bound evidence artifacts. Focused tests 10/10 and verifier self-test 19/19 pass; exact CI `33701131998` passes all product gates on `116e9341`. Parent capacity proof remains open and unclaimed. |
| BSB-SCALE-ARTIFACT-READBACK-001 | High | DONE | Schema v3 requires an explicit real artifact root and reads back all six role-bound files before the final evidence claim. Missing, tampered, size-mismatched, duplicate, traversing and symlinked files fail closed; focused tests 12/12 and verifier self-test 25/25 pass in exact CI `33705358061` on `75fd1adf`. Parent capacity proof remains open and unclaimed. |
| BSB-SCALE-REPORT-BINDING-001 | High | DONE | Schema v4 requires every real report to embed the exact release identity, role/run identity and role-specific measurement summary under canonical SHA-256 bindings. Unrelated, empty, unstructured or contradictory report bytes fail closed; focused tests 13/13 and verifier self-test 27/27 pass in exact CI `33709162435` on `42909989`. Parent capacity proof remains open and unclaimed. |
| BSB-SCALE-SIGNER-ATTESTATION-001 | High | DONE | Schema v5 requires every complete report to carry a post-run Ed25519 attestation from the approved workload/provider/platform owner. A separately controlled trust store is bound to the exact release identity; missing, stale, self-substituted, wrong-role or invalid signatures fail closed. Focused tests 14/14 and self-test 31/31 pass in exact CI `33758851931` on `be50c905`; parent capacity proof remains open and unclaimed. |
| BSB-SCALE-SIGNER-SEPARATION-001 | High | DONE | Schema v6 / trust schema v2 require distinct workload/provider/platform approval-owner IDs and distinct normalized Ed25519 public-key fingerprints. Shared-key aliases and reused owner identities fail closed. Expected-red shared-key attack 0/1, focused regression 1/1 and self-test 32/32 pass in exact CI `33786591571` on `386166d1`; parent capacity proof remains open and unclaimed. |
| BSB-SCALE-TRUST-STORE-SEPARATION-001 | High | DONE | Real CLI verification requires the physical non-symlink trusted-signer store to resolve outside the artifact root, so an evidence bundle cannot supply its own trust anchors. Expected-red attack 0/1, focused regression 2/2 and self-test 32/32 pass in exact CI `33792626690` on `7d50d8e1`; parent capacity proof remains open and unclaimed. |
| BSB-SCALE-TRUST-STORE-PINNING-001 | High | DONE | Real CLI verification requires an independently supplied SHA-256 of the exact out-of-band trusted-signer bytes and emits the verified digest. A substituted store fails closed before signer authorization. Expected-red attack 0/1, focused regression 3/3 and self-test 32/32 pass in exact CI `33797481152` on `aaf9ba59`; parent capacity proof remains open and unclaimed. |
| BSB-SCALE-PRE-RUN-APPROVAL-BINDING-001 | High | DONE | Trust schema v3 hashes the complete pre-run approval plan: approval/environment, population/concurrency/RPS/phases, endpoint and CDN limits, resilience/saturation/alert drills and rollback limit. Approval-reference, target-RPS and latency-limit substitutions fail closed. Expected-red 0/1, focused regression 3/3 and self-test 33/33 pass in exact CI `33805319296` on `760a0be1`; parent capacity proof remains open and unclaimed. |
| BSB-SCALE-VERIFICATION-CLOCK-001 | High | DONE | Verifier metadata and real-artifact readback capture actual UTC verification time and reject a future `run.completedAt` or any future-dated post-run Ed25519 attestation. Expected-red 0/1, focused regression 1/1 and self-test 35/35 pass in exact CI `33809586680` on `af9aa168`; parent capacity proof remains open and unclaimed. |
| BSB-MID-FEATURE-RESTORE-001 | High | DONE | A primed natural-Base feature at durable cursor 69 is observed during transient `live-restoring` without polling. Exact Chromium sends no duplicate play, persists only remaining cursors 70–84 exactly once, settles once and reaches the exact $1000.00 win / $1999.00 balance ready shell. Exact run `33517845045`; broader provider/manual evidence remains in `BSB-EVIDENCE-001`. |
| BSB-ACTIVE-FEATURE-SKIP-001 | High | DONE | Exact Chromium invokes Skip during an active natural-Base feature, persists all 51 durable cursors exactly once in order, sends one play and one settlement, remains write-stable after completion, and reaches the exact $1000.00 win / $1999.00 balance ready shell in 969ms. Exact run `33527487460`; broader provider/manual evidence remains in `BSB-EVIDENCE-001`. |
| BSB-REPLAY-TIMER-LIFECYCLE-001 | High | DONE | Exact Chromium performs six consecutive Play Again cycles from one cached Replay GET. Every cycle returns to one identical 49-cell completed presentation, drains active presentation timers to zero, peaks at two concurrent timers and performs zero authenticate/play/event/settlement writes. Exact run `33533314785`; broader provider/manual evidence remains in `BSB-EVIDENCE-001`. |
| BSB-AUTH-TIMEOUT-RECOVERY-001 | High | DONE | Exact Chromium stalls the first authenticate transport until the production client's 10-second AbortController timeout fires. `RGS_TIMEOUT` is visible and fail-closed; explicit reload performs one successful authenticate and zero play/event/settlement writes. Exact run `33536390205`; broader provider/manual evidence remains in `BSB-EVIDENCE-001`. |
| BSB-RGS-TEARDOWN-ABORT-001 | High | DONE | Every pending live RGS transport is now owned and aborted on controller destroy and full page teardown; lifecycle abort remains distinct from timeout and the client stays reusable. Exact Chromium reloads during a stalled authenticate, observes one app-owned abort, recovers with one successful authenticate and sends zero play/event/settlement writes. Exact run `33553904771`; broader provider/manual evidence remains in `BSB-EVIDENCE-001`. |
| BSB-REPLAY-TEARDOWN-ABORT-001 | High | DONE | Every pending read-only Replay transport is now owned and aborted on controller destroy and full page teardown; lifecycle abort remains distinct from timeout and the client stays reusable. Exact Chromium reloads during a stalled Replay GET, observes one app-owned abort, recovers with one exact queryless GET and sends zero authenticate/play/event/settlement writes. Exact run `33563146222`; broader provider/manual evidence remains in `BSB-EVIDENCE-001`. |
| BSB-SOCIAL-REPLAY-001 | High | DONE | Exact-package Social Replay covers math-backed loss, positive Base win, complete Deep Access feature and 10,000x max-win books in ready/completed states. All four use exact SC display, one read-only Replay GET, zero wallet/event writes and zero official restricted-term hits. Exact run `33463575542`; checklist row 38 human final-surface sign-off remains open. |
| BSB-ASSET-002    | High     | DONE  | Decode → reveal → two-frame paint barrier is runtime- and screenshot-gated. Exact `fbfe4ed` artifacts render penguin/vault pixels at 1920x1080 and 1366x768 plus the intended responsive plate at 390x844, 844x390 and Replay; all geometry paint states are `painted`. |
| BSB-README-001   | Medium   | DONE  | README now matches the M3 live/replay lifecycle, repository commands, exact-package workflow and `QA_BLOCKED` release-truth boundaries. Three regressions prevent stale greybox/no-paid-play/no-Replay claims and command drift. Commit `68ddaca`; final harness-bound evidence `77faaa5`. |
| BSB-PACKAGE-CLEAN-001 | Medium | DONE | Production build now removes only the generated `build/` root before Vite, emits an exact commit/tree/clean identity, and packaging fails closed when that identity is missing, malformed or stale. An injected stale bundle is removed; repeated builds each contain 10 files and one JS bundle. Exact package/browser gates pass in run `33453194613`. |
| BSB-GUIDE-001 | High | DONE | Game Information renders one immutable ten-entry control guide covering pointer/touch, keyboard, Space, Escape, Sound, access level, Base Amount, Normal/Turbo/Reduced, Skip, primary action, confirmation and dialog closure. Exact Chromium maps every visible game control to the guide and proves all contract keys are visible; checklist row 29 retains manual final reconciliation. Run `33456368436`. |
| BSB-ORIENTATION-001 | High | DONE | One authenticated 390x844 → 844x390 → 390x844 live round trip proves responsive vault-plate swaps, square 49-cell board, centered/unclipped HUD, focus, >=44px hittable controls, zero play/checkpoint/settlement writes and portrait geometry return within 0.5 CSS px. Exact run `33461309637`; checklist row 27 physical-device sign-off remains open. |
| BSB-MOTION-PREF-001 | High | DONE | Normal/Turbo presentation speed is stored under one versioned contract and restored across reloads. System reduced-motion remains authoritative and disabled, does not erase the chosen speed, and releasing the override resumes Turbo. Storage failures and invalid values fall back safely; exact mobile browser evidence performs four authentications with zero wallet/event writes. Checklist row 29 retains manual reconciliation. |
| BSB-GRID-A11Y-001 | High | DONE | The board exposes one named 7x7 ARIA grid with seven explicitly owned rows and exact one-based row/column indices on all 49 cells. Exact-package geometry retains the square board across desktop, laptop, portrait, small phone, tablet, landscape, orientation round trip and Replay. Run `33471069057`; real screen-reader/device approval remains open. |
| BSB-TIMER-A11Y-001 | High | DONE | Jurisdiction Session Position is an atomic polite status while the once-per-second Session Time is a named non-live timer. Exact mobile Chromium proves `00:00 → 00:01`, `$0.00 → +$7.00`, one play and no settlement/event writes. Run `33474573594`; real screen-reader/device approval remains open. |
| BSB-TOUCH-001 | High | DONE | Exact mobile Chromium performs a real board double-tap with visual viewport fixed at 1x, then touch-drags Game Information from scrollTop 0 to 1022px while document scroll and zoom remain fixed. One authenticate and zero play/settlement/event writes; run `33478922886`. Real iOS/Android sign-off remains open. |
| BSB-BLACKOUT-PERF-001 | High | DONE | Repaint-heavy full-stage brightness/saturation filters were replaced with phase-scoped opacity compositing. Exact before/after Chromium improves BLACKOUT p95 `50.0 → 33.4ms` and >50ms frames `4 → 1`; max stays 66.7ms. Lock/shutter geometry and required desktop/mobile/Replay surfaces pass in run `33483678009`; real-device pacing remains open. |
| BSB-CHARACTER-LAYER-001 | High | DONE | The Vaultkeeper no longer reserves `transform, filter` compositing in Idle, Monitoring or Reduced Motion. Exact Chromium proves active reactions retain the hint and idle/skip cleanup return to `auto`; 71 scenarios / 1855 checks pass in run `33486188541`. Real-device memory/pacing approval remains open. |
| BSB-TIMER-CADENCE-001 | Medium | DONE | Session Time now uses one drift-resistant 1,000ms interval instead of a 250ms poll, reducing nominal UI callbacks from 240 to 60 per minute. Exact Chromium records one callback over 1,100ms while visible time, authoritative `$7.00` position and wallet request limits remain exact; 71 scenarios / 1856 checks pass in run `33489360248`. Real-device battery/memory savings are not claimed. |
| BSB-AUDIO-MUTE-LIFECYCLE-001 | High | DONE | Muting now stops and disconnects every active game-owned voice plus the ambience graph instead of retaining silent Web Audio sources. Unmute restores exactly one ambience graph; a repeated FULL → MUTE cycle returns to zero voices/ambience without stacking. Exact current-package evidence is recorded in run `BS-20260901-14`; final approved assets and manual listening/clipping/device QA remain in parent `BSB-AUDIO-001`. |
| BSB-AUDIO-VISIBILITY-MUTE-001 | High | DONE | Persisted/current mute remains source-free during initial unlock and hidden → visible Web Audio suspend/resume. Exact Chromium proves `MUTED`, stored `0`, 0 voices and 0 ambience after resume; explicit LOW unmute creates exactly one graph. Exact run `33506751334`; final approved assets and manual listening/clipping/device QA remain in parent `BSB-AUDIO-001`. |
| BSB-AUDIO-VISIBILITY-RACE-001 | High | DONE | Hidden/visible Web Audio operations are serialized so a delayed suspend cannot complete after an early resume. Expected-red unit proof previously ended `suspended`; exact Chromium now completes one delayed suspend plus one following resume and ends visible, `running`, persisted-muted and source-free. Exact run `33511226684`; final approved assets and listening/clipping/device QA remain in parent `BSB-AUDIO-001`. |
| BSB-AUDIO-VOICE-GAIN-LIFECYCLE-001 | High | DONE | Every game-owned voice oscillator now owns and disconnects its paired GainNode on mute, eight-voice-cap eviction and natural completion. Exact Chromium drives an active UI voice plus ambience into mute, reduces active graph edges `5 → 1`, increases gain disconnects `9 → 12`, and leaves zero voices/ambience with only master-to-destination retained. Exact run `33545045810`; final approved assets and listening/clipping/device QA remain in parent `BSB-AUDIO-001`. |
| BSB-SPACE-REPEAT-001 | High | DONE | A held Space key can no longer initiate a second paid round after the first returns to ready. Exact Chromium produces one initial and one repeat keydown but only one exact Base `/wallet/play`, with zero event/settlement writes and clean diagnostics; run `33499016878`. |
| BSB-ENTER-REPEAT-001 | High | DONE | Held Enter on the focused primary action retains one initial native activation while repeat keydown is default-prevented. Exact Chromium observes initial unprevented plus repeated prevented Enter, one exact Base `/wallet/play`, and zero event/settlement writes; run `33503792855`. |

Do not split a run across unrelated open items. After two failed attempts without new evidence, record the blocker and move to the next safe item.
