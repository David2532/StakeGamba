# BLACKSITE seven-day sprint

- Sprint window: 2026-08-30 through 2026-09-05 (Europe/Berlin calendar days)
- Current sprint day: 6
- Lifecycle: `QA_BLOCKED_EXTERNAL_AND_MANUAL`
- Release candidate ready: no

## Definition of Done evidence

| Area                                     | Current evidence                                                                                                                                                                                   |
| ---------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Production build                         | PASS locally and in exact current-head CI `33824176688` for source commit `021a1d64`, exit 0 |
| Lint                                     | PASS locally and in exact current-head CI `33824176688`, exit 0 |
| Typecheck                                | PASS locally and in exact current-head CI `33824176688` — production `svelte-check`, `checkJs: true`, 0 errors / 0 warnings |
| Automated app tests                      | PASS locally and in exact current-head CI `33824176688` — 254/254, exit 0; validator self-test 46/46 PASS |
| Math/package invariants                  | Current exact CI PASS — 7/7 math tests, 300,000 books, fingerprint `d03fab…78d8`. Package generation was skipped by the current pull-request workflow; last exact package remains 6 files / 455,088 bytes with tree `eb9d20f6…2348d` and is not relabelled as current-source evidence. |
| Current-head browser/E2E                 | PASS for source candidate `021a1d64` — exact CI `33824176688` passes 97/97 scenarios and 3123/3123 checks. A denied storage getter reaches `live-ready` at 390x844 with defaults, one authentication, zero paid writes and clean diagnostics. |
| Desktop/portrait/landscape visual review | PASS for automated candidate scope — artifact `9919710838` supplied 1920x1080, 390x844, 844x390 and storage-denied 390x844 views, manually inspected without overlap, crop, scroll or broken images. Real-device sign-off remains external. |
| Live wallet/replay/restore               | PARTIAL — real auth-timeout abort/reload, dynamic/ambiguous authenticate parameters, fractional new-play/restore, uncertain paid-play reload/restore, server-accepted/client-response-lost play and checkpoint recovery, zero- and positive-payout settlement response-loss recovery without retry/double credit, event-driven feature cursor restore, active-feature Skip, auth/session expiry, failed settlement, `ERR_IPB`, competing-input deduplication, exact currency families and Social Replay surfaces are current-package browser proven; broader provider evidence remains |
| Gameplay state matrix                    | PARTIAL — exact current-package proof covers five deterministic rule wins per canonical mode plus natural Base and confirmed Deep Access feature entry/cycles/exit/ready cleanup; the full requested production interaction matrix is not yet current-head proven |
| Player HUD/responsiveness/accessibility  | PASS for the current automated control surface — computed centering, uncut labels, >=44px touch geometry, safe viewport placement, real double-tap/no-zoom plus touch-scroll behavior, 3px/2px keyboard focus, named route/presentation groups, atomic state/net-position statuses, a named non-live timer, exact high-cost descriptions and a ten-entry visible interaction guide pass exact-package checks; real screen-reader/device sign-off remains open |
| Animation/cinematics                     | PARTIAL — all authored Base win tiers retain verified Normal/Turbo evidence; every feature board snapshot preserves `bonus_idle`, Reduced Replay retains zero-motion cleanup, and a bounded 3.9s fresh-live boot intro has skip plus Reduced Motion bypass. Approved Spine clips and real-device pacing/memory remain open |
| Audio                                    | PARTIAL — one procedural master bus, seven-stop reel cadence, semantic Vaultkeeper/feature recipes, priority ambience duck/restore, gesture unlock, mute/level persistence, serialized visibility suspend/resume, cooldown and voice cap are unit/browser proven. Every owned voice oscillator and gain disconnects on mute/cap/completion; mute leaves only the master destination edge, delayed rapid hidden → visible ends running in event order, persisted mute stays source-free, explicit unmute restores exactly one graph, and repeated cycles remain source-free; final approved assets and manual clipping/listening/device QA remain open |
| Product identity/assets/licenses         | PARTIAL — original penguin fallback and separate desktop/portrait base-vault plates are hash/provenance-bound, optimized and exact-browser integrated; human approval, cleanup/Spine, BLACKOUT and foreground layers remain open |
| Stake/provider checklist                 | PARTIAL — all 51 rows are exactly mapped: 20 automated PASS, 18 automated-proof/manual-open, 5 manual-only, 6 external and 2 N/A; all 38 automated references resolve on exact current-head package/browser identity, while 23 manual gates and 6 external approvals remain explicitly unclaimed |
| Release blockers                         | OPEN — known critical/high blockers remain; do not claim `RELEASE_CANDIDATE_READY`                                                                                                                 |

Run `BS-20260904-21` closes a startup-resilience hole relevant to privacy-restricted mobile browsers. Reading the `window.localStorage` property itself could throw before the previous method-level guards ran, blocking motion, audio and intro initialization. The runtime now acquires storage through one fail-safe adapter and degrades persistence to defaults. Expected-red focused tests failed 2/6; final focused tests pass 6/6 and the integrated suites pass 14/14. Local frozen install, lint, 0/0 typecheck, 254 tests, build, 46/46 scale self-test and 7/7 math over 300,000 books pass. Exact CI `33824176688` passes all executed gates and 97/97 Chromium scenarios / 3123 checks on source `021a1d64`; artifact `9919710838` binds the storage-denied capture and three target views. Package/resolver steps were skipped by that pull-request workflow path, so the previous exact package identity remains the last package claim. Physical iPhone/Safari and infrastructure capacity remain unclaimed.

Run `BS-20260904-20` closes the iPhone-demo candidate's exact-browser regression. The first exact run `33814835895` passed every non-browser gate but exposed six harness assumptions: authentication was misclassified as a wallet write, a compact-omitted character was queried for desktop-only motion CSS, and two focus checks inherited intro state. The focused correction changes only Chromium QA. Exact CI `33818861870` now passes frozen install, lint, 0/0 typecheck, 204 tests, build, 35/35 scale self-test, 7/7 math over 300,000 books, package readback, 93/93 Chromium scenarios / 3035 checks and current-SHA 51-point resolution on `c4eeb96b`. Artifact `9917922010` supplied five inspected views; compact layouts issue zero Vaultkeeper image resources while desktop renders one. This is client/package evidence only; public-link reachability, physical iPhone behavior and production-equivalent infrastructure capacity remain unclaimed.

Run `BS-20260904-19` closes the scale-evidence verification-clock gap without changing product runtime. Expected-red proved a future `run.completedAt` was accepted. The verifier now captures its actual UTC time, rejects a run completed after that instant, rejects every report attestation signed after it, and emits the verification timestamp in metadata/readback results. Focused regression passes 1/1 and self-test 35/35. Exact CI `33809586680` passes frozen install, lint, 0/0 typecheck, 197 tests, build, 7/7 math over 300,000 books, package readback, 89/89 Chromium scenarios / 2878 checks and current-SHA 51-point resolution on `af9aa168`; artifact `9914780746` supplied four inspected target views. This proves verifier behavior only; production-equivalent capacity and external/manual approvals remain open.

Run `BS-20260903-18` closes mutable pre-run scale-plan authorization without changing product runtime. Expected-red proved that an approval reference could be changed after trust-store creation; follow-up review showed workload targets and latency limits shared the same weakness. Trust schema v3 now hashes the complete approved plan and rejects changed approval metadata, target RPS or endpoint limits. Focused regression passes 3/3 and self-test 33/33. Exact CI `33805319296` passes frozen install, lint, 0/0 typecheck, 196 tests, build, 7/7 math over 300,000 books, package readback, 89/89 Chromium scenarios / 2878 checks and current-SHA 51-point resolution on `760a0be1`; artifact `9913304478` supplied four inspected target views. This proves verifier governance only; production-equivalent capacity and external/manual approvals remain open.

Run `BS-20260903-17` closes the scale trust-store substitution gap without changing product runtime. Expected-red proved the real CLI accepted an out-of-band trust-store file despite an explicitly wrong digest. It now requires a 64-character SHA-256 pin from release control, hashes the exact bytes before parsing or signer authorization, fails closed on mismatch and emits the verified digest. Focused regression passes 3/3 and self-test 32/32. Exact CI `33797481152` passes frozen install, lint, 0/0 typecheck, 195 tests, build, 7/7 math over 300,000 books, package readback, 89/89 Chromium scenarios / 2878 checks and current-SHA 51-point resolution on `aaf9ba59`; artifact `9910339166` supplied four inspected target views. This proves verifier governance only; production-equivalent capacity and external/manual approvals remain open.

Run `BS-20260903-16` closes the scale trust-store boundary gap without changing product runtime. Expected-red proved the real CLI accepted a trust store inside the untrusted artifact root. It now requires physical non-symlink paths, canonicalizes them and rejects a trust store at or below the evidence root. Focused regression passes 2/2, self-test 32/32, local frozen install/lint/typecheck/194 tests/build and 7/7 math over 300,000 books pass. Exact CI `33792626690` passes package readback, 89/89 Chromium scenarios / 2878 checks and current-SHA 51-point resolution on `7d50d8e1`; artifact `9908532046` supplied four inspected target views. This proves fail-closed verifier governance only; production-equivalent capacity and external/manual approvals remain open.

Run `BS-20260903-15` closes the scale-signer separation gap without changing product runtime. The expected-red fixture proved schema v5 accepted workload and provider approvals signed by the same private key behind different signer aliases. Schema v6 / trust schema v2 now normalize every Ed25519 key to SPKI DER, reject duplicate SHA-256 key fingerprints and require distinct workload/provider/platform owner IDs. The focused regression passes 1/1 and the self-test 32/32. Local frozen install, lint, 0/0 typecheck, 193 tests, build and 7/7 math over 300,000 books pass. Exact CI `33786591571` passes the same gates, six-file package/readback, 89/89 Chromium scenarios / 2878 checks and current-SHA 51-point resolution on `386166d1`; artifact `9906288764` supplied four manually inspected target views. This proves fail-closed evidence governance only; production-equivalent capacity and all external/manual approvals remain open.

Run `BS-20260903-13` advances the positive accepted-checkpoint response-loss proof without changing product runtime. The expected-red contract failed 0/1 and the implemented mapping passes 1/1; local frozen install, lint, 0/0 typecheck, 192/192 tests and build pass. Exact attempts `33774863738` and `33777051665` pass install through package/math but fail one Chromium harness assertion at 88/89 scenarios / 2658/2659 checks: the play response had already exposed the cursor intended to become authoritative only after reload, so the client correctly skipped it as already persisted. Candidate `85dcadfe` separates a fresh cursorless play round from the cursor-bearing reauthenticated restore round and passes the focused/full app regression locally. Exact Chromium, resolver and scenario captures remain required, so the item and release status stay blocked. Four primary views from artifact `9902484244` were inspected without visual regression; no product visual source changed.

Run `BS-20260903-14` closes that proof on the unchanged candidate. Exact CI `33779286645` passes every gate, including 89/89 Chromium scenarios / 2878 checks and current-SHA 51-point resolution. The exact browser evidence records request order beginning `authenticate → play → event → authenticate`, then the 48 remaining ordered checkpoints and one final settlement: 2 authentications, 1 play, 49 distinct events and 1 end-round. One accepted first-checkpoint response is intentionally aborted during reload, never rewritten, and the authoritative `$200.00` result settles once at `$1199.00`. Artifact `9903557420` binds the six-file `d3d61694…f1eda` frontend and supplies four primary plus both pending/restored views, manually inspected without visual regression. Product runtime, math, wallet/provider schema, assets and dependencies are unchanged; production-equivalent capacity and external/manual approvals remain open.

Run `BS-20260903-10` closes the scale-evidence authenticity gap without changing product runtime. Schema v5 requires a release-identity-bound trust store and post-run Ed25519 attestation over each complete report plus canonical binding. Workload, provider and platform ownership is enforced per role, and missing attestations, stale signatures, substituted public keys and wrong-role signers fail closed. The expected-red regression proved schema v4 accepted a self-asserted rehashed report at 13/14; final focused tests pass 14/14 and the self-test 31/31. Local gates pass 192 app tests, build, 7/7 math over 300,000 books and isolated package verification. Exact CI `33758851931` passes the same gates, six-file package/readback, 88/88 Chromium scenarios / 2454 checks and the 51-point resolver on `be50c905`; artifact `9895164200` supplied four manually inspected target views. This proves validator behavior and client-package quality only; production-equivalent infrastructure capacity remains unproven.

Run `BS-20260903-12` closes the positive accepted-play response-loss proof without changing product runtime. The corrected exact package reauthenticates the accepted `base_big` round at the debited `$999.00` balance, restores 49 distinct authoritative cursors, presents the `$200.00` win and settles once at `$1199.00`; exact order and bodies prove no duplicate play, checkpoint or payout. Because session position is defined from the post-reload authentication baseline, `−$200.00` is correct and the inherited `−$199.00` harness expectation was fixed. Local focused and full gates pass, including 192 tests, build, 31/31 scale self-test, 7/7 math over 300,000 books and package readback. Exact CI `33767664586` passes 88/88 Chromium scenarios / 2651 checks and the 51-point resolver on `bc260688`; artifact `9898887055` supplied four primary views and both recovery states for manual inspection. External infrastructure and manual approvals remain open.

Run `BS-20260903-11` advances the positive accepted-play response-loss proof without changing product runtime. The authoritative `base_big` round models one accepted `$1.00` play whose response is lost, then restores 49 durable cursors and one `$200.00` settlement with exact endpoint bodies and order. Local frozen install, lint, 0/0 typecheck, 192 tests, build, 31/31 scale self-test and 7/7 math over 300,000 books pass. Exact run `33763426664` passed those gates and failed only because the new QA assertion targeted the optional session-position element while the mock jurisdiction left it disabled. Correction `bfb3e844` explicitly enables that display contract; local focused tests pass, while exact CI `33765392559` is still running. The item and visual review therefore remain open and no new release-readiness claim is made.

Run `BS-20260903-09` closes the positive-payout settlement-response-loss gap without changing product runtime. Exact Chromium executes the authoritative `base_big` `$200.00` win, persists all 49 durable cursors once, lets the mock RGS accept one `/wallet/end-round`, then loses only its client response during reload. Reauthentication adopts the authoritative inactive `$1199.00` balance with no settlement retry or double payout; attempts/acceptances/aborts are 1/1/1. The first exact CI correctly failed because the new harness expected one rather than 49 checkpoints; the corrected exact cursor/order assertion passes in CI `33731906526` with 88/88 scenarios / 2454 checks, six-file package/readback and current-SHA 51-point resolution on `104bf9a5`. Artifact `9884742420` supplied four primary views and both pending/restored states for inspection. The 1,000,000-user infrastructure claim and external/manual approvals remain open.

Run `BS-20260903-08` closes the accepted-checkpoint response-loss evidence gap without changing product runtime. A controlled exact-package transport lets the native mock RGS accept exactly one `/bet/event` cursor `blacksite-book-events-v1:2`, withholds its response until reload aborts the client transport, then returns the authoritative active round at that cursor. Chromium proves exact order `authenticate → play → event → authenticate → end-round`, authentication/play/event/settlement counts 2/1/1/1, checkpoint attempts/acceptances/aborts 1/1/1, no checkpoint rewrite, final `$999.00` balance and `$0.00` win. Local gates pass 191 tests, build, scale self-test 27/27 and 7/7 math over 300,000 books. Exact CI `33724943687` passes the same gates, six-file package/readback, 87/87 Chromium scenarios / 2432 checks and the 51-point resolver on commit `36f0f7a5`; artifact `9882091742` supplied four primary views plus pending/restored states for manual inspection. The 1,000,000-user infrastructure claim and external/manual approvals remain open.

Run `BS-20260903-07` closes the accepted-settlement response-loss evidence gap without changing product runtime. A controlled exact-package transport allows the native mock RGS to accept exactly one `/wallet/end-round` with body `{sessionID}`, withholds its response until reload aborts the client request, then returns the authoritative inactive `$999.00` session on reauthentication. Chromium proves exact order `authenticate → play → event → end-round → authenticate`, request counts 2/1/1/1, settlement attempts/acceptances/aborts 1/1/1, no retry or duplicate payout, final `$999.00` balance and cleared win. Local gates pass 191 tests, build, scale self-test 27/27 and 7/7 math over 300,000 books. Exact CI `33720790794` passes the same gates, six-file package/readback, 86/86 Chromium scenarios / 2406 checks and the 51-point resolver on commit `8e777caf`; artifact `9880546915` supplied four primary views plus pending/restored states for manual inspection. The 1,000,000-user infrastructure claim and external/manual approvals remain open.

Run `BS-20260903-06` closes the accepted-play response-loss evidence gap without changing product runtime. A controlled exact-package transport allows the native mock RGS to accept and record one `$1.00` Base play, withholds its response until reload aborts the client request, then returns the authoritative active zero round on reauthentication. Chromium proves exact order `authenticate → play → authenticate → end-round`, request counts 2/1/1 with zero event writes, one accepted response and one intentional `net::ERR_ABORTED`, final `$999.00` balance / `$0.00` win, and no play resubmission or duplicate settlement. Local gates pass 191 tests, build, scale self-test 27/27 and 7/7 math over 300,000 books. Exact CI `33714953239` passes the same gates, six-file package/readback, 85/85 Chromium scenarios / 2384 checks and the 51-point resolver on commit `04669203`; artifact `9878505321` supplied four primary views and the restored state for manual inspection. The 1,000,000-user infrastructure claim and external/manual approvals remain open.

Run `BS-20260903-05` closes the scale-report binding gap. Schema v4 requires all six load/CDN/provider-ledger/resilience/observability/rollback artifacts to be structured JSON and embed the exact release identity, role/run identity and role-specific top-level measurement summary. Canonical SHA-256 bindings reject unrelated, empty, unstructured or contradictory report bytes even if their declared file digest is internally correct. The expected-red test proved that schema v3 accepted contradictory content at 12/13; final focused tests pass 13/13 and the self-test 27/27. Local gates pass 191 app tests, build and 7/7 math over 300,000 books. Exact CI `33709162435` passes the same gates, a six-file package/readback, 84/84 Chromium scenarios / 2358 checks and the 51-point resolver on commit `42909989`; artifact `9876572820` supplied four manually inspected target views. This proves validator behavior and exact client-package quality only; production-equivalent infrastructure capacity remains unproven.

Run `BS-20260903-04` closes the scale-artifact readback gap. Schema v3 separates metadata validation from the final claim and requires the CLI to read the six declared load/CDN/provider-ledger/resilience/observability/rollback files from an explicit root. It rejects missing, tampered, size-mismatched, duplicate, traversing and symlinked files before issuing `EXTERNAL_SCALE_EVIDENCE_VALIDATED`. The expected-red test proved metadata-only CLI acceptance, then final focused tests pass 12/12 and the self-test 25/25. Local gates pass 190 app tests, build and 7/7 math over 300,000 books. Exact CI `33705358061` passes the same gates, a six-file package/readback, 84/84 Chromium scenarios / 2358 checks and the 51-point resolver on commit `75fd1adf`; artifact `9875363496` supplied four manually inspected target views. This proves validator behavior and exact client-package quality only; production-equivalent infrastructure capacity remains unproven.

Run `BS-20260903-03` closes the adversarial scale-evidence integrity gap. Schema v2 rejects approval after run start, a run window shorter than declared phases, endpoint/request-total contradictions, duplicate or missing evidence roles, cross-run artifacts and non-positive artifact sizes. The focused regression began 9/10 with the expected late-approval failure and finishes 10/10; local gates pass 188 app tests, build, 19/19 verifier self-tests and 7/7 math over 300,000 books. Exact CI `33701131998` passes the same gates, a six-file package/readback, 84/84 Chromium scenarios / 2358 checks and the 51-point resolver on commit `116e9341`. Artifact `9873890152` is SHA-bound. This validates evidence integrity only; production-equivalent infrastructure capacity remains unproven.

Run `BS-20260903-01` closes the active-mode badge acceptance blocker. A bounded two-column stage heading and truncation-safe chip now have stable geometry hooks; one expected-red contract became 1/1 PASS. Exact CI `33692233510` passes 178 tests, build, 7/7 math over 300,000 books, six-file package/readback, 84/84 Chromium scenarios / 2358 checks and the 51-point resolver. Active Deep Access geometry measures 12px separation and zero collisions at both desktop sizes; the redundant chip is intentionally hidden in compact portrait/landscape while selected route and active mode remain visible elsewhere. Four current-head captures were inspected. The release remains blocked by production-equivalent infrastructure load proof and external/manual asset, audio, device, rights, Creative and Stake acceptance.

Run `BS-20260902-46` hardens the exact static-package boundary without changing gameplay runtime, math, wallet/provider schema, product assets or dependencies. The previous exact-CI package contained 10 files / 712,434 bytes; the pinned inline shell now ships exactly 6 files / 439,506 bytes, removing 272,928 bytes (38.31%) of unreachable generated residue while retaining recovery metadata. One classic inline runtime, asset/source digests, physical package roots, clean commit/tree identity, canonical manifest/README/toolchain fields and bounded embedded-data/debug/source-map scans are regression-bound. Two consecutive local full package verifications each pass 300,000 books; 177 app tests and an independent red team pass. Exact current-head CI `33685146602` passes install, lint, typecheck, tests, build, full math, package verification, 84/84 Chromium scenarios / 2328 checks and the 51-point resolver. Artifact `9868237907` supplies inspected current-head target-view and active-feature screenshots. The client package improvement does not prove CDN/RGS/provider capacity for up to 1,000,000 users; `BSB-SCALE-001` requires production-equivalent load, cache, latency, error, idempotency and observability evidence.

Run `BS-20260902-45` fixes phase ownership of the Vaultkeeper idle stance. Prior exact evidence contained 23 Base `monitoring` regressions inside each Natural Base and Deep Access feature segment. Exact run `33669021386` now observes, per profile, 12 static `bonus_idle` snapshots and 11 bounded `bonus_win` reactions with zero Monitoring before recovery; authored idle transform/filter, animation `none` and compositor `auto` pass. The same run passes 142 tests, build, 7/7 math over 300,000 books, isolated package/readback, the 51-point resolver and 84/84 Chromium scenarios / 2328 checks. Manual inspection also exposed a pre-existing Deep Access chip/header collision, recorded as the next high visual blocker; visual DoD is downgraded to partial until fixed.

Run `BS-20260902-44` closes the blocked Reduced Motion Replay evidence slice. Exact run `33656188904` passes every gate on `60e6562`: 141 tests, build, 7/7 math over 300,000 books, package/readback, current-SHA 51-point resolver and 84/84 Chromium scenarios / 2326 checks. Reduced Replay completes in 50.3ms; four sampled frames remain board `idle`, Vaultkeeper `idle_a`, profile `reduced`, CSS `none` / `0s`, compositor `auto`, with one Replay GET and zero wallet calls. Artifact `9857355056` supplies inspected 1366x768, 1920x1080, 390x844 and 844x390 captures without overlap, crop, scroll or broken-image regression.

Run `BS-20260902-43` added the three-profile cached Replay proof and correctly remained blocked while the exact corrected run was pending. Focused evidence began 29/30 with the expected missing contract and ended 30/30 plus syntax PASS; the first exact run reached `replay-completed` / `reduced` and exposed only a stale final Turbo postcondition. Run 44 resolves that evidence blocker without further product-code change.

Run `BS-20260902-42` closes the only unmeasured Base tier/profile cell without changing runtime, math, wallet, assets or dependencies. Deterministic live QA now switches from Turbo Small/Big to Normal Big, binds the selected profile to `win_big`, verifies the exact 1,400ms computed clip, observes 1,584.2ms semantic ownership, captures the active frame and retains exact `$2.07` payout authority. Exact run `33649656438` passes 141 tests, 7/7 math over 300,000 books, package/readback, the 51-point resolver and 84/84 Chromium scenarios / 2324 checks.

Run `BS-20260902-41` closes the profile-specific Feature-win evidence gap without changing runtime, math, wallet, assets or dependencies. Natural Base now deliberately exercises Normal while confirmed Deep Access exercises Turbo; both assert the selected control profile, computed `bonus_win` animation, compositor ownership and bounded semantic exit. Initial exact run `33643364848` measured a valid 528.7ms Normal window and exposed only an overly tight 500ms QA cap. The corrected 650ms bound passes in exact run `33645610063`: 280ms CSS / 528.0ms observed Normal and 110ms / 177.3ms Turbo, with 141 tests, 7/7 math over 300,000 books, package/readback, 51-point resolver and 84/84 Chromium scenarios / 2317 checks.

Run `BS-20260902-40` closes the Base Small/Medium/Big evidence gap. Delayed exact run `33627655297` proved the tier runtime and exposed only a stale Replay bound: the new 420ms Small reaction produced 451.9ms but QA still capped the old profile at 420ms. Replay assertions now explicitly follow the authored Small tier and are contract-locked. Exact run `33637682367` passes 141 tests, 7/7 math gates over 300,000 books, package/readback, the 51-point resolver and 84/84 Chromium scenarios with 2317/2317 checks. Current screenshots and wallet/network isolation pass; no runtime, math, wallet, asset or dependency changed in this run.

Run `BS-20260902-37` closes the missing bonus-specific character response. Feature-phase wins now own `bonus_win` while Base wins retain `win_acknowledge`; CSS is bounded at 280ms Normal / 110ms Turbo / 0ms Reduced. Exact natural Base and confirmed Deep Access flows observe 204.2ms / 178.2ms Turbo ownership, exact `$1000.00` / `$2000.00` payouts, one play each and zero checkpoint/event/settlement writes. Run `33621532438` passes all package, math, browser and 51-point gates.

Run `BS-20260902-39` advances the blocked Base-win-tier slice through every non-browser gate on the current product tree: frozen install, lint, 0/0 typecheck, 141 tests, production build, 7/7 math gates across 300,000 books, and isolated package generation/readback all pass. The package contains 10 frontend files / 712,356 bytes with tree `5393337b…58e2e`; math remains 7 files / 48,697,667 bytes with tree `6bd0c4c7…01da` and fingerprint `d03fab…78d8`. GitHub still exposes no workflow/status, and both normal and extended Playwright downloads failed on CDN timeout/truncated zero-byte archives, so current-head Chromium/screenshots and the browser-bound resolver remain blocked.

Run `BS-20260902-38` implements the contract-required Base Small/Medium/Big character tiers and deterministic exact-centi-x selector. Local frozen install, lint, typecheck, 141 tests and production build pass; focused expected-red evidence began 0/1 and the final focused contracts pass 6/6 plus the timer-backed mapping case. GitHub accepted exact commit `b4211f6`, but exposed no workflow run/status and local Chromium is absent, so current-head package, browser, wallet/network and visual evidence are explicitly blocked and the slice remains open.

Run `BS-20260902-36` gives authoritative zero outcomes a distinct, bounded Vaultkeeper loss acknowledgement instead of reusing the feature-recovery response. Normal computes 320ms and is observed for 318.7ms; Turbo computes 130ms and is observed for 129.4ms before idle. Reduced Motion is immediate, feature recovery is not duplicated, and two exact Base plays remain `$0.00` with zero checkpoint/event/settlement writes. Run `33612508384` passes all package, math, browser and 51-point gates.

Run `BS-20260902-35` closes the direct Vaultkeeper win-acknowledgement timing gap without changing runtime or math. Exact Chromium measures the semantic character state and computed animation together: Turbo computes 110ms and remains in `win_acknowledge` for 190.3ms; Normal computes 280ms and remains for 464.8ms before Monitoring. Dedicated hit captures, three exact Base requests and zero checkpoint/event/settlement writes remain authoritative. Run `33605467946` passes all package, math, browser and 51-point gates.

Run `BS-20260902-34` closes the staggered board-reveal timing evidence gap without changing runtime or math. Exact Chromium measures first/final cell computed styles and phase boundaries: Normal 180ms animation with 0–156ms stagger remains visible for 359.8ms, and Turbo 70ms with 0–54ms stagger remains visible for 128.4ms. Both final cells complete before phase exit; two exact Base requests finish at `$0.00` / `$998.00` with zero checkpoint/event/settlement writes. Run `33601180214` passes all package, math, browser and 51-point gates.

Run `BS-20260902-33` closes the direct feature-tease timing evidence gap without changing runtime or math. Exact Chromium measures Vaultkeeper `feature_tease` and the board `anticipation` lock pulse from computed styles and presentation transitions: both own 600ms Normal / 180ms Turbo and remain visible for complete 752.8ms / 233.5ms windows. Two exact Base requests and zero checkpoint/event/settlement writes remain authoritative. Run `33597126878` passes all package, math, browser and 51-point gates.

Run `BS-20260902-32` closes the direct cascade phase-timing evidence gap without changing runtime or math. Exact Chromium measures hit/remove/drop/settle from computed styles and presentation transitions: Turbo 110/55/105/35ms CSS → 124.5/56.9/104.3/40.2ms; Normal 280/150/250/90ms CSS → 312.6/152.1/249.4/92.3ms. Both profiles retain 16.8ms p95, zero >50ms samples, three exact Base requests and zero checkpoint/event/settlement writes. Run `33593132383` passes all package, math, browser and 51-point gates.

Run `BS-20260902-31` closes the direct BLACKOUT hero-timing evidence gap without changing runtime or math. Exact Chromium executes deterministic `blackout_zero` in Normal and Turbo, measures trigger/recovery at 1000.4/1026.9ms and 359.7/365.8ms against 1000/360ms CSS, verifies two exact 1,000,000-micro-unit BLACKOUT requests, exact `$0.00` and zero checkpoint/event/settlement writes. Run `33588488559` passes all package, math, browser and 51-point gates.

Run `BS-20260902-30` completes the semantic Reduced Motion contract for the Vaultkeeper. The explicit reduced profile now disables animation and transitions and releases compositor hints without depending on the separate media-query fallback. Exact Chromium activates `feature_trigger` and measures `none` / `0s` / `auto` while retaining the stored Turbo preference and performing zero wallet writes; run `33584733683` passes all package, math, browser and 51-point gates.

Run `BS-20260902-29` removes stale Replay-only timing overrides. Default Replay playback now defers step and hit duration to the active Normal/Turbo/Reduced profile while preserving explicit non-negative overrides for tests/embedders. Exact Chromium measures 311.9ms Normal and 122.5ms Turbo hit ownership across two cached plays, one Replay GET and zero wallet writes; run `33580462193` passes all package, math, browser and 51-point gates.

Run `BS-20260831-25` corrected the delayed-Actions diagnosis from run 24: the previously cancelled source run was resumed and passed exactly, then the byte-identical three-file patch was fast-forwarded as `b94c059` and verified again on current HEAD. This closes the bounded live natural-Base/confirmed-Deep-Access browser-evidence gap without claiming the still-open human wording review.

Run `BS-20260831-26` attempted the next interrupted-feature restore proof. Frozen install, lint, typecheck, 106 tests, build, full math and package generation passed in both exact source runs, but Chromium timed out twice on two distinct transient-state observations in the new scenario. The slice was reverted after the second attempt; DoD evidence is unchanged and the final product tree matches the previously verified `30cf194` tree exactly.

Run `BS-20260831-27` moved to a distinct provider-recovery gap and proved it end to end. An accepted play whose first settlement returns HTTP 503 remains active, makes no automatic retry, exposes explicit `RELOAD / RESTORE`, reauthenticates once and performs exactly one successful second settlement. Exact CI run `33432657970` passes 106 tests, full math/package gates and 61 Chromium scenarios / 1472 checks without changing runtime, math, assets or dependencies.

Run `BS-20260831-28` proves the distinct API-level `ERR_IPB` recovery path. A balance race rejects one legal play, remains fail-closed without automatic retry, then explicit reload discovers and settles the authoritative active zero-win round exactly once. Exact CI run `33436799735` passes 107 tests, full math/package gates and 61 Chromium scenarios / 1490 checks without changing runtime, math, assets or dependencies.

Run `BS-20260831-29` proves the distinct expired-session path. API `ERR_SESSION` rejects one play fail-closed, explicit reload performs one reauthentication without resubmitting that action, and only a subsequent deliberate click sends one new successful play. Exact CI run `33439810616` passes 108 tests, full math/package gates and 62 Chromium scenarios / 1518 checks without changing runtime, math, assets or dependencies.

Run `BS-20260831-30` proves the distinct expired-settlement-session path. After one accepted play and checkpoint, API `ERR_SESSION` leaves settlement fail-closed without automatic retry; explicit reload reauthenticates once and completes the restored active round once without duplicate play/checkpoint. Exact CI run `33442885524` passes 108 tests, full math/package gates and 63 Chromium scenarios / 1550 checks without changing runtime, math, assets or dependencies.

Run `BS-20260901-01` proves the ambiguous authenticate-step path. A nominally successful response with conflicting official `stepBet` and supported `minStep` aliases is visibly rejected as `STEP_BET_CONFLICT`; the exact package keeps paid play unavailable after one authenticate and sends no play, event or settlement writes. Exact CI run `33448128514` passes 109 tests, full math/package gates and 64 Chromium scenarios / 1558 checks without changing runtime, math, assets or dependencies.

Run `BS-20260901-02` closes the highest-impact automated day-3 semantic gap. Access-level and motion controls are explicitly named groups, launch/board updates are atomic polite statuses, and high-cost confirmation binds the 4×/$4.00 or 80×/$80.00 explanation to the dialog. Exact CI run `33450641637` passes 112 tests, full math/package gates and 64 Chromium scenarios / 1576 checks; visual layout remains unchanged and real screen-reader/device approval remains open.

Run `BS-20260901-03` closes the generated-frontend provenance gap. Production build now removes only its generated root before Vite, writes exact commit/tree/clean identity, and packaging fails closed for stale or foreign output. An injected stale bundle is removed and repeated builds each contain 10 files with one JS bundle; exact CI run `33453194613` passes 112 tests, full math/package gates and 64 Chromium scenarios / 1576 checks without changing runtime UI, math, assets or dependencies.

Run `BS-20260901-04` closes the automated interaction-guide gap behind checklist row 29. One immutable ten-entry contract now documents every visible control plus pointer/touch, keyboard, Space and Escape behaviour; Game Information renders it, and exact Chromium proves both directions of the control/guide mapping. Exact CI run `33456368436` passes 113 tests, full math/package gates and 64 Chromium scenarios / 1579 checks; manual final-control reconciliation remains explicitly open.

Run `BS-20260901-05` closes the automated orientation-transition gap behind checklist row 27. One authenticated mobile context now performs 390x844 → 844x390 → 390x844 while proving responsive asset swaps, square 49-cell board, centered HUD, focus, >=44px controls, zero wallet writes and portrait return within 0.5 CSS px. Exact CI run `33461309637` passes 114 tests, full math/package gates and 65 Chromium scenarios / 1739 checks; physical-device sign-off remains explicitly open.

Run `BS-20260901-06` closes the automated Social Replay outcome-matrix gap behind checklist row 38. Math-backed loss, positive Base win, complete Deep Access feature and exact 10,000x max-win books are scanned in ready and completed states for official restricted terms and dollar-prefixed Social display; each performs one Replay GET and no wallet/event write. Exact CI run `33463575542` passes 115 tests, full math/package gates and 69 Chromium scenarios / 1821 checks; the required human final-surface sign-off remains explicitly open.

Run `BS-20260901-07` closes persisted presentation-speed behavior behind checklist row 29. Normal/Turbo is stored through one versioned contract; reduced-motion overrides and disables the control without erasing Turbo, and Turbo resumes when the override clears. Exact CI run `33468510345` passes 119 tests, full math/package gates and 70 Chromium scenarios / 1832 checks; four authentications produce no play, settlement or event write, while manual final-control reconciliation remains open.

Run `BS-20260901-08` closes the automated board-topology accessibility gap. The visible board is now one named 7x7 ARIA grid with seven owned rows and exact one-based row/column positions on every cell; exact geometry proves the structural wrapper preserves the square 49-cell layout across all eight surfaces. Exact CI run `33471069057` passes 120 tests, full math/package gates and 70 Chromium scenarios / 1843 checks; real screen-reader and physical-device approval remain open.

Run `BS-20260901-09` closes a jurisdiction-readout accessibility defect. Session Position remains an atomic polite status, while Session Time is now a labelled non-live timer instead of forcing a screen-reader announcement every second. Exact mobile Chromium proves the timer still advances `00:00 → 00:01`, the authoritative position updates `$0.00 → +$7.00`, and the one-play path emits no settlement/event write. Exact CI run `33474573594` passes 121 tests, full math/package gates and 70 Chromium scenarios / 1845 checks; real screen-reader/device sign-off remains open.

Run `BS-20260901-10` closes checklist row 28's CSS-only interaction-evidence gap. Exact 390x844 Chromium performs two real board taps and one real touch drag: viewport scale and page position remain fixed, while Game Information scrollTop moves `0 → 1022` inside its own 1767px surface. Exact CI run `33478922886` passes 122 tests, full math/package gates and 71 Chromium scenarios / 1854 checks; real iOS/Android sign-off remains open.

Run `BS-20260901-11` removes a measured BLACKOUT repaint hotspot. The full-stage vault plate now animates phase-scoped opacity instead of brightness/saturation filters; exact before/after Chromium improves p95 `50.0 → 33.4ms` and >50ms frames `4 → 1` while max remains 66.7ms. Exact CI run `33483678009` passes 123 tests, full math/package gates and 71 Chromium scenarios / 1854 checks; visual identity is retained and real-device pacing remains open.

Run `BS-20260901-12` bounds the Vaultkeeper layer lifecycle. The image no longer reserves `transform, filter` in Idle, Monitoring or Reduced Motion; authored active reactions retain the hint, and skip cleanup returns it to `auto`. Exact CI run `33486188541` passes 124 tests, full math/package gates and 71 Chromium scenarios / 1855 checks; pixels remain unchanged and real-device memory/pacing remains open.

Run `BS-20260901-13` bounds the optional jurisdiction timer cadence. Session Time now configures one 1,000ms interval instead of one 250ms interval, reducing nominal callbacks from four to one per second while retaining `Date.now()`-based elapsed time. Exact Chromium observes one callback over 1,100ms, visible `00:00 → 00:01`, exact `$0.00 → +$7.00` position and no settlement/event writes. Exact CI run `33489360248` passes 124 tests, full math/package gates and 71 Chromium scenarios / 1856 checks; real-device battery/memory claims remain open.

Run `BS-20260901-14` closes a Web Audio mute-lifecycle leak. Previously zero gain left the ambience oscillator and active voices alive; mute now stops/disconnects every source, clears voice/cooldown ownership, and unmute recreates exactly one ambience graph. Exact Chromium proves mute `0/0`, LOW unmute `1` ambience, a repeated FULL → MUTE cycle `0/0`, and muted reload `0`; exact CI run `33493907726` passes 124 tests, full math/package gates and 71 Chromium scenarios / 1858 checks. Final approved assets and manual listening/clipping/device QA remain open.

Run `BS-20260901-15` closes a paid-input repetition defect. Holding Space across round completion previously left repeated keydowns eligible to invoke the next ready action; the live handler now consumes but rejects `event.repeat`. Exact Chromium observes `[false, true]`, remains at one exact Base play after returning to ready and sends no event/settlement writes; exact CI run `33499016878` passes 125 tests, full math/package gates and 71 Chromium scenarios / 1860 checks.

Run `BS-20260901-16` closes the corresponding focused-primary native-key repetition defect. Initial Enter retains native activation, while repeated Enter is default-prevented before a second click can be synthesized after ready. Exact Chromium records initial unprevented plus repeated prevented Enter, one exact Base play and no event/settlement writes; exact CI run `33503792855` passes 126 tests, full math/package gates and 72 Chromium scenarios / 1871 checks.

Run `BS-20260901-17` closes a muted visibility-lifecycle source leak. Persisted/current mute no longer creates an ambience oscillator during initial unlock or hidden → visible context resume; exact Chromium retains `MUTED`, stored `0`, 0 voices and 0 ambience after suspend/resume, then explicit LOW unmute creates exactly one graph. Exact CI run `33506751334` passes 127 tests, full math/package gates and 72 Chromium scenarios / 1872 checks. Final approved assets and manual listening/clipping/device QA remain open.

Run `BS-20260901-18` closes an asynchronous visibility ordering defect. A delayed hidden suspend could previously finish after an immediate visible resume and strand the visible slot in `suspended`; lifecycle operations are now serialized and destroyed directors reject queued work. Exact Chromium delays suspend by 80ms, records one completed suspend plus one following resume and ends `running`/`MUTED` with zero sources. Exact CI run `33511226684` passes 128 tests, full math/package gates and 72 Chromium scenarios / 1873 checks. Final approved assets and manual listening/clipping/device QA remain open.

Run `BS-20260901-19` closes the deferred mid-feature restore evidence gap with event-triggered observation. A primed natural-Base feature at durable cursor 69 is visible during `live-restoring`; exact Chromium sends no duplicate play, writes only cursors 70–84 once in order, performs one settlement and reaches the authoritative $1000.00 win / $1999.00 balance ready shell. Exact CI run `33517845045` passes 128 tests, full math/package gates and 73 Chromium scenarios / 1952 checks without changing runtime, math, assets or dependencies.

Run `BS-20260901-20` closes the active-feature Skip/settlement evidence gap. One authenticated natural-Base feature is skipped during `hit`; exact Chromium persists all 51 durable cursors once in order, performs one play and one settlement, remains write-stable after completion, and reaches `$1000.00` win / `$1999.00` balance in 969ms. Exact CI run `33527487460` passes 128 tests, full math/package gates and 74 Chromium scenarios / 2174 checks without changing runtime, math, assets or dependencies.

Run `BS-20260901-21` closes the repeated-Replay timer-lifecycle evidence gap. Exact Chromium replays one cached read six consecutive times, returns to one identical authoritative completed presentation each time, drains all active presentation timeouts after every cycle, peaks at only two concurrent timers and performs zero authenticate/play/event/settlement writes. Exact CI run `33533314785` passes 129 tests, full math/package gates and 75 Chromium scenarios / 2182 checks without changing runtime, math, assets or dependencies.

Run `BS-20260901-22` closes the browser-level authentication-timeout recovery evidence gap. Exact Chromium lets the production 10-second AbortController cancel one stalled authenticate transport, shows bounded `RGS_TIMEOUT` with paid play unavailable, then explicit reload authenticates once and performs no play/event/settlement write. Exact CI run `33536390205` passes 130 tests, full math/package gates and 76 Chromium scenarios / 2201 checks without changing runtime, math, assets or dependencies.

Run `BS-20260901-23` closes a retained Web Audio graph leak. Voice oscillators now own paired gain nodes and disconnect both on mute, eight-voice-cap eviction and natural completion; the expected-red gain assertion reproduced the defect first. Exact Chromium measures graph edges `5 → 1` on mute, gain disconnects `9 → 12`, zero voices/ambience and no orphan voice edge. Exact CI run `33545045810` passes 131 tests, full math/package gates and 76 Chromium scenarios / 2202 checks; final approved assets and manual listening/clipping/device QA remain open.

Run `BS-20260901-24` closes a live-session transport-lifecycle gap. The client owns and aborts every pending request on controller/page teardown, distinguishes lifecycle cancellation from the 10-second timeout, and remains reusable. Exact Chromium reloads during a stalled authenticate, records one app-owned abort, performs one successful recovery authenticate and zero play/event/settlement writes. Exact CI run `33553904771` passes 133 tests, full math/package gates and 77 Chromium scenarios / 2214 checks; broader provider and human approval evidence remains open.

Run `BS-20260901-25` closes the matching read-only Replay transport-lifecycle gap. The client owns and aborts every pending Replay GET on controller/page teardown, distinguishes lifecycle cancellation from the 10-second timeout, and remains reusable. Exact Chromium reloads during a stalled Replay read, records one app-owned abort, performs one exact queryless recovery GET and zero authenticate/play/event/settlement writes. Exact CI run `33563146222` passes 135 tests, full math/package gates and 78 Chromium scenarios / 2223 checks; broader provider and human approval evidence remains open.

Run `BS-20260902-26` closes a Max-Win character-timing cut. The semantic `max_win` state previously inherited a generic 32ms Normal / 12ms Turbo cue delay despite the authored CSS clips lasting 1,000ms / 360ms, so recover replaced the hero reaction almost immediately. Presentation timing now owns the complete bounded clip and remains skippable. Exact Chromium measures 1006.2ms Normal and 364.3ms Turbo, exact `$10000.00` payout authority, 79 scenarios / 2231 checks and clean diagnostics in run `33567927794`; approved Spine clips and physical-device pacing remain open.

Run `BS-20260902-27` closes the matching terminal Recovery cut. `settled` previously replaced the 1,000ms / 360ms authored `recover` clip after a generic 32ms / 12ms delay; the focused regression measured 33.3ms. Recovery now owns the complete bounded window without double-delaying a preceding feature exit and remains skippable. Exact Chromium measures 995.7ms Normal and 357.1ms Turbo, exact `$10000.00` payout authority, 79 scenarios / 2233 checks and clean diagnostics in run `33572370281`; approved Spine clips and physical-device pacing remain open.

Run `BS-20260902-28` closes the Turbo spin-start timing cut. The director previously advanced to Monitoring after 60ms while the authored Vaultkeeper reaction lasts 110ms; the expected-red regression measured 59.6ms. Turbo now owns the complete bounded 110ms window, Normal remains 160ms, Reduced remains immediate and Skip retains timer ownership. Exact Chromium measures 111.7ms Turbo and 165.0ms Normal, exact `$10000.00` payout authority, 79 scenarios / 2235 checks and clean diagnostics in run `33576342359`; approved Spine clips and physical-device pacing remain open.

## Day milestones

Run `BS-20260903-02` adds the repository-side gate needed to accept—but not fabricate—production-scale evidence. `scripts/blacksite-scale-evidence.mjs` and nine contract tests reject wrong commit/package identity, mocked or non-production-equivalent targets, missed approved concurrency/RPS, latency/cache/saturation breaches, wallet or settlement duplicates, incomplete resilience drills, missing correlated observability/alert acknowledgement, failed rollback and invalid artifact digests. Its 14/14 synthetic self-test validates the verifier only; it does not prove capacity. Exact current-head CI `33696034709` passes 187 tests, the scale gate, full math/package, 84/84 Chromium scenarios / 2358 checks and the 51-point resolver. No product runtime, layout, math, wallet/provider schema, asset, dependency or lockfile changed. Parent `BSB-SCALE-001` remains open until workload/provider/platform owners execute and approve a SHA-bound production-equivalent CDN/RGS/provider run.

- Day 1: baseline, memory/runbook, build/test/math status, and top release blockers captured. CI/typecheck/Chromium are clean; responsive HUD, penguin/vault environments and authoritative cascade presentation are exact-browser integrated. Proceed to rig/BLACKOUT and remaining cinematic completion.
- Day 2: wallet/math/state-machine/replay/restore regression closure.
- Day 3: HUD, centered controls, responsiveness, accessibility, and mobile.
- Day 4: production reels/cascades, penguin, vault/lock cinematics, and frame pacing.
- Day 5: production audio, assets, load time, and memory behavior.
- Day 6: broad current-head E2E, browser/mobile/visual/Stake regression and red-team review.
- Day 7: candidate freeze, complete regression, licenses/docs/release notes, and evidence-backed readiness verdict.

Critical defects override the day theme. Advance early when a milestone is actually evidenced.
