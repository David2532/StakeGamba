# Backend scaffold

This folder contains a minimal example backend demonstrating provably-fair endpoints.

- `server.js`: Example Express server that exposes `/server-seed-hash` and `/reveal-server-seed`.

Usage:

```bash
cd backend
npm init -y
npm install express
node server.js
```

Replace with your secure implementation for production and follow Stake Engine docs for exact API requirements.
